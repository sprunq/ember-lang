def main() {
    let a = 0;
    a += 20;
    let b = false;
    let c = (a+20) < 100;
    while c == false {
        a += 1;
    }
    function_to_call(20,false)
}

def function_to_call(a: int, b : bool) -> int {
}

